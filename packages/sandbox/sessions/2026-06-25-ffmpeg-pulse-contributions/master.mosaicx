{
  "kind": "mosaicx_document", "version": 1, "m0": "F", "assets": {},
  "size": { "width": 1920, "height": 1080 }, "fps": 30, "durationMs": 9000,
  "sources": [ { "type": "template_invocation", "templateId": "@m0saic/hero/ffmpeg-pulse/contributions/v1", "props": {} } ],
  "suggestedOutput": { "dir": "<sandbox>/sessions/2026-06-25-ffmpeg-pulse-contributions", "basename": "master" },
  "agent": { "note": "MASTER — @m0saic/hero/ffmpeg-pulse/contributions/v1 (heatmap beat). APPROVED one-shot. Shared pulse-chrome wrapping a GitHub-style contribution heatmap (7 weekday rows x 8 trailing weeks, green intensity = theme.primaryBright) + a 2x2 activity rail; cohesive theme.card panels. Driven by the new pulse.activity.grid (API: /stats/commit_activity). Aspect-adaptive. 6 unit tests; internal beat (skips CLI E2E per precedent). Preview + postmortem set. Eyebrow BEAT 5 provisional — runner assigns final order (pairs with activity-trend)." }
}
