{
  "kind": "mosaicx_document",
  "version": 1,
  "m0": "F",
  "assets": {},
  "size": {
    "width": 1920,
    "height": 1080
  },
  "fps": 30,
  "durationMs": 14900,
  "sources": [
    {
      "type": "template_invocation",
      "templateId": "@m0saic/dsl-tutorial/v1",
      "props": {
        "M0String": "2(2(3[1,1,1],1),3[2(1,1),1,2[1,1]])",
        "speedMultiplier": 1,
        "preset": "dark",
        "title": "Animated Wireframe"
      }
    }
  ],
  "agent": {
    "note": "**M0b — IDE shell + chrome, wired to the real pipeline.** New template `@m0saic/dsl-tutorial/v1` (+ internal `@m0saic/dsl-tutorial/chrome/v1`).\n\n**Live now:** full-bleed **header** (traffic lights · title · *Tutorial* chip · **Speed** pill · **Step N / 15** pill that **counts up** as it plays) and **status bar** (● Parsing · Tokens · Nodes) — both are coordinated subtemplates rendered at their exact slot size on the parent's global clock. The four content panels are **labeled placeholders** (filled in M1–M4).\n\n- Shell fills **all W/H** (no floating window), per your M0a note.\n- Duration is **auto-derived** from step-count × speed (15 steps → ~15s here).\n- Debugger will show **only real trace values** (cols/rows/tile.w/h) — no synthetic gap/pad.",
    "question": "Two checks before I build the panels: (1) does the **chrome** read premium — header element sizing/spacing, the counting **Step** pill, the status bar — and (2) is the **panel grid** right (proportions, all W/H used)? Flip `preset` to `\"light\"` and change `speedMultiplier` to feel the knobs.",
    "context": {
      "category": { "kind": "other", "label": "ide-shell-chrome" },
      "intent": { "action": "propose", "summary": "M0b — template scaffold + chrome + step pill on the real pipeline" },
      "relation": { "kind": "refines", "target": "001" }
    }
  }
}
