{
  "kind": "mosaicx_document",
  "version": 1,
  "m0": "F",
  "assets": {},
  "size": {
    "width": 1920,
    "height": 1080
  },
  "fps": 30,
  "durationMs": 1000,
  "suggestedOutput": {
    "dir": ".",
    "basename": "master"
  },
  "agent": {
    "note": "**SHIPPED base ☑ (gate 29 — WIREFRAME pack opener; `@m0saic/wireframe/base/v2`).** Promoted from take 4 (`master_candidate-04.mosaicx`): white default canvas, explicit debug defaults (mode/labelContent/labelOverflow/unlabeledCells/background/nullFrames/nullLabel/fill), the 2×2 grid default face, NO cover (dev template ruling), grid-paper lattice wired (`utils/paperGrid.ts`), user-facing description, `format` png/rgba, agent-scoped `labelsByStableKey`/`cellColors`. Dev-diagram feature shipped alongside: `nullFrames: \"show\"` = layer explode (emit:multi, one PNG per overlay depth, `-` holes dashed + muted) + `nullLabel` (\"null\" | \"-\"); the passthrough lattice (stress-13/14 + -a) was REMOVED by ruling (\"using layout is a better result in most cases\"). Four engine/CLI fixes rode the gate (usage-card gap on optional image props; trivial-wrapper format/bg inheritance; .mosaic-tier doc alpha; alpha-root child carrier). Previews: preview.png re-minted at the 2×2 white default + corner-M (BR). Ledger closeout performed 09-04 after the ship commit; if the commit title was not meant as the ship verdict, revert master.* and reopen candidate-04.\n\nOriginal take-4 note follows.\n\n---\n\n**TAKE 4 — white default** (founder on take 3, with Make screenshots: \"transparent bg is poor when the background is also black. we need to default to white\").\n\n- `background` defaults to **white** (`DEFAULT_BACKGROUND` + `defaultProps`); the Background segmented control now opens on White. Transparent stays one click away for trace/overlay work — the Layout page's Transparent / trace styles already pass it explicitly, so they are unchanged.\n- Side effect worth knowing: the perf-heatmap PNGs (electron, no background prop) now sit on white instead of transparent.\n- Grid default, explicit debug defaults, preset-decides-mode, grid-paper lattice, and the four engine/CLI fixes all stand. No cover.\n\nStress twins re-frozen AND stills re-rendered (01/02/03/05/06 now sit on white; 04 already asked for white).",
    "question": "White default reads right? Then stress-01..10. Still open: core-vs-publisher (rec. CORE) · internal:true? (rec. no) · grid-paper keep/prune · label → \"Wireframe\"?",
    "response": {
      "verdict": "ship",
      "text": "take 4 white default + stress-01..12 → founder commit a1ab72de \"ship wireframe v2 to prod\" (09-03; preceded by c0628eb2 \"v1 of wireframe to release\"). Still unruled in-file: nullLabel default (\"null\" kept; stress-12 asked whether \"-\" should be it), stress-11-a layer-explode verdict — both committed as history in the ship commit.",
      "via": "git commit message — transcribed by agent (no in-file response was saved)"
    }
  },
  "sources": [
    {
      "type": "template_invocation",
      "templateId": "@m0saic/wireframe/base/v2",
      "props": {}
    }
  ]
}
