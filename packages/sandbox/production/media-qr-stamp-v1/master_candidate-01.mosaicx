{
  "kind": "mosaicx_document",
  "version": 1,
  "m0": "F",
  "assets": {},
  "size": {
    "width": 1280,
    "height": 720
  },
  "fps": 30,
  "durationMs": 5000,
  "suggestedOutput": {
    "dir": ".",
    "basename": "master_candidate-01"
  },
  "agent": {
    "note": "**v1 PROD GATE — `@m0saic/media/qr/stamp/v1` \"QR Stamp\"** (gate 23 — media pack remainder 6/n; capability tier). The PRIMITIVE wrapper over `stampQrOnMedia` — the same engine as the free-tier post-render watermark wrap (battle-tested via §7.7 tier testing). Props = showcase: reel-10s + defaults (br corner, 18%, auto mode, fade-in).\n\n| Stage | Current | Proposed |\n|---|---|---|\n| Flags | capability, not internal, not deprecated, not primitive-flag (it IS the primitive by design — naming only) | confirm |\n| Tags | brand, qr, stamp, custom | confirm |\n| Cover | none — first open = error card (required mediaPath) + MAKE disabled | **needed? cheap here**: the stamp engine composes over ANY base doc purely — a synthetic scenic base + the stamped QR + welcome band is ~an hour. Or rule none. |\n| Tutorial | none | skip? |\n| Battery | **11/11 GREEN, ZERO template bugs** (3rd zero-bug gate) | — |\n| Scannability | founder phone: the stamp at 18% of 720p ≈ 130px — scan 01-default from screen | scan |\n\n**Battery coverage:** default video (auto→light on bright scene, verified visually) · image base → still-video per the mp4 format hint · corner tl · size extremes (50% + margin 0; 1%) · dark / transparent / auto-with-explicit-buckets · animation none · missing mediaPath → visible error card · format-hint mp4 + `-o .png` → loud mismatch.\n\n**Judgment item:** the format hint pins mp4 ALWAYS — an image input can't emit a PNG (`-o .png` errors loudly). The bespoke still/video templates split this; the primitive is video-only by hint. Accept (primitive = video deliverable) or drop the hint so image inputs can go png (highlights-style per-render container)?\n\nUnit 55/55 (stamp family suites).",
    "question": "Showcase verdict? Phone-scan the stamp? Cover ruling (cheap synthetic offer above)? mp4-only format hint accept or split? Then stress.",
    "response": {
      "verdict": "revise",
      "text": "a lot of the knobs on this template dont seem to work",
      "via": "chat — transcribed by agent"
    }
  },
  "sources": [
    {
      "type": "template_invocation",
      "templateId": "@m0saic/media/qr/stamp/v1",
      "props": {
        "mediaPath": "<sandbox>/production/media-highlights-v1/fixtures/reel-10s.mp4"
      }
    }
  ]
}