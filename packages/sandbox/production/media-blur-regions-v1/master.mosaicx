{
  "kind": "mosaicx_document",
  "version": 1,
  "m0": "F",
  "assets": {},
  "size": {
    "width": 1280,
    "height": 720
  },
  "fps": 30,
  "durationMs": 3000,
  "suggestedOutput": {
    "dir": ".",
    "basename": "master"
  },
  "agent": {
    "note": "**v1 PROD GATE \u2014 `@m0saic/media/blur-regions/v1` \"Easy Blur\"** (gate 19 \u2014 media pack remainder 2/n; chart: `.ai/LAUNCH/v1-template-cut.md`). Props = showcase: talk-720p fixture + two blur regions. This gate doubles as the open founder review from the canvas-regions sprint (Phases 1-4 done 08-17).\n\n| Stage | Current | Proposed |\n|---|---|---|\n| Flags | not internal, not deprecated, not primitive | confirm |\n| Gate | \u2014 | **base** |\n| Preview assets | committed set \u2014 untouched (default look unchanged) | confirm |\n| Cover / Tutorial | cover BUILT this gate (no source = error card was the first-open face; now a synthetic scenic demo with two pixelated regions + band, adaptive px-clamped) | eyeball cover; tutorial needed? |\n\n**Pre-audit verdict: GENUINELY WELL-BUILT \u2014 zero template bugs found.** 15/15 input-driven battery over minted fixtures (testsrc2 patterns so effect placement is VISIBLE + sine audio): passthrough base case, blur/pixelate placement exact, image input, bare-px + canvas-wrapped + m0-flavor regions all land, overshoot clamps, fully-outside region fail-fasts with a VISIBLE error card (\"regions are never silently dropped\" \u2014 the privacy posture holds end-to-end), all three geometries (inset/exact/mask), 4K, rot90 display-coordinate mapping correct, 12-region mask single-pass, strength extremes, **audio intact** (ffprobe: video+audio streams on the blurred output).\n\n**Notes:** pixelate on flat-color content is visually a no-op by nature (blocks average to the source) \u2014 correct, just subtle on synthetic fixtures. New reusable tool: media-aware freeze-twin (ffprobes media props into ctx.media) \u2014 gate 13's ad-hoc version is now durable in scratchpad.\n\nJest 39/39 (2 suites). NOT primitive (media utility).",
    "question": "Default/showcase look verdict? Cover OK? Tutorial needed? Flags confirmed? Then stress.",
    "response": {
      "verdict": "ship",
      "text": "showcase pass (\"looks good, stress\") -> stress x10 pass -> \"all 10 pass, ship it\"",
      "via": "chat \u2014 transcribed by agent",
      "note": "SHIPPED base \u2611 (gate 19, media pack remainder 2/n). ZERO template bugs found across pre-audit (15 configs) + stress (x10) \u2014 the canvas-regions sprint's open founder review is now CLOSED via this gate. Cover built (synthetic scenic demo + pixelated regions, adaptive band). NOT primitive. Previews (91617ccf) untouched. Privacy posture verified end-to-end (clamp vs fail-fast, visible error card, audio intact)."
    }
  },
  "sources": [
    {
      "type": "template_invocation",
      "templateId": "@m0saic/media/blur-regions/v1",
      "props": {
        "sourceId": "<sandbox>/production/media-blur-regions-v1/fixtures/talk-720p.mp4",
        "regions": {
          "canvas": {
            "w": 1280,
            "h": 720
          },
          "regions": [
            {
              "x": 160,
              "y": 90,
              "w": 320,
              "h": 200
            },
            {
              "x": 800,
              "y": 400,
              "w": 360,
              "h": 240
            }
          ]
        }
      }
    }
  ]
}
