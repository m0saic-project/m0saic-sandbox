{
  "kind": "mosaicx_document",
  "version": 1,
  "m0": "F",
  "assets": {},
  "size": {
    "width": 1920,
    "height": 1080
  },
  "fps": 30,
  "durationMs": 4000,
  "suggestedOutput": {
    "dir": "./renders",
    "basename": "10-all-audio-inputs"
  },
  "agent": {
    "note": "**STRESS 10/10 — 10-all-audio-inputs** (gate-36 stress set for `@m0saic/media/scroll_wall/v1`).\n\nTHE SILENCE PROMISE, WITH EVERY INPUT CARRYING AUDIO at 1920x1080. Six clips, ALL of them audio-bearing (reel-10s and talk-720p, three times each — so repeats also exercise the seeded stagger). A wall of clips playing their own sound at once is noise, so v1 is silent by design: every source is muted individually AND the document now stamps `audio: {mode:\"off\"}` once. Watch: the output file must have NO audio stream at all — not a silent one.",
    "question": "Pass or fail? Note anything off and I'll fix + re-mint this variant."
  },
  "sources": [
    {
      "type": "template_invocation",
      "templateId": "@m0saic/media/scroll_wall/v1",
      "props": {
        "clips": [
          "<sandbox>/production/media-highlights-v1/fixtures/reel-10s.mp4",
          "<sandbox>/production/media-blur-regions-v1/fixtures/talk-720p.mp4",
          "<sandbox>/production/media-highlights-v1/fixtures/reel-10s.mp4",
          "<sandbox>/production/media-blur-regions-v1/fixtures/talk-720p.mp4",
          "<sandbox>/production/media-highlights-v1/fixtures/reel-10s.mp4",
          "<sandbox>/production/media-blur-regions-v1/fixtures/talk-720p.mp4"
        ],
        "rows": 2
      }
    }
  ]
}
